apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  labels:
    app: todo-app
spec:
  selector:
    app: todo-app
  type: NodePort   # or NodePort if running locally (e.g., minikube)
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
